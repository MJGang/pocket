import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
{{#if plugins}}
import {{#each plugins}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}{{/each}} from 'unplugin-vue-components/vite'
{{/if}}

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [
    vue(),
    {{#if plugins}}
    {{#each plugins}}
    {{this}}(),
    {{/each}}
    {{/if}}
  ],
  // 其他配置...
})
